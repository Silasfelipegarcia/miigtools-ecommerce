<section class="miig-section miig-section--featured miig-section--products" aria-labelledby="miig-featured-heading">
  <header class="miig-section-head text-center text-md-start">
    <h2 id="miig-featured-heading" class="miig-section-title">{{ heading_title }}</h2>
    <p class="miig-section-lead mb-0">{{ text_featured_lead }}</p>
  </header>
  <div class="row{% if axis == 'horizontal' %} row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4{% endif %} g-3 g-md-4">
  {% for product in products %}
    <div class="col">{{ product }}</div>
  {% endfor %}
  </div>
</section>
