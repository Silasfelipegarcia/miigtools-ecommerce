<div id="cookie">
  <div class="container">
    <div class="row">
      <div class="col-12 text-center">
        <p class="mb-2">{{ text_cookie }}</p>
        <div class="miig-cookie-actions">
          <button type="button" value="{{ agree }}" class="btn bg-primary text-white">{{ button_agree }}</button>
          <button type="button" value="{{ disagree }}" class="btn btn-light">{{ button_disagree }}</button>
        </div>
      </div>
    </div>
  </div>
</div>